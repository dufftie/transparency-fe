import { Button } from 'antd';

const SupportButton = () => {
  return (
    <Button className="support-button" type="primary" size="large" href="/support">
      Support
    </Button>
  );
};

export default SupportButton;